{
  "private": true,
  "scripts": {
    "generate:seo": "node scripts/generate-seo-pages.js",
    "sync:cycletime": "node scripts/sync-cycletime.js",
    "sync:catalog": "npm run sync:cycletime && npm run generate:seo"
  },
  "dependencies": {
    "cheerio": "1.0.0"
  }
}
