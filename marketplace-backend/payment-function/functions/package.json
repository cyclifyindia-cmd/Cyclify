{
  "name": "cyclify-payment-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "check": "node --check index.js && node --check provider-adapter.js && node --check whatsapp-notifier.js && npm test",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "razorpay": "^2.9.8"
  }
}
